Thread:
  columns:
    title:
      type: string(255)
      notnull: true

Post:
  actAs:
    Solr:
      fields:
        title:
          name: title_t
          boost: 2
        body: body_t

  columns:
    thread_id:
      type: integer
      notnull: true
    title:
      type: string(255)
      notnull: true
    body:
      type: clob
      notnull: true
  relations:
    Thread:
      onDelete: CASCADE
      foreignAlias: Posts
